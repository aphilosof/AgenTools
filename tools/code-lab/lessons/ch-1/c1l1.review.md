# Adversarial review — `c1l1.draft.md` (Output and `print`)

Reviewed against `lessons/schema.md`, `CURRICULUM-STRUCTURE.md`, `AUTHORING.md`,
and the finalized brief `lessons/ch-1/c1l1.brief.md`. All Python claims were
re-executed on `/opt/homebrew/bin/python3.13` (Python 3.13.7).

**Verdict: clean.** The draft is accurate, coherent, complete against the brief,
and adheres to schema and structure. No blocking issues. A few low-priority
observations are recorded at the end; none require a change.

---

## 1. Accuracy (verified on Python 3.13.7)

Every `code` / `solution` / `starter` was executed. All matched their stated
`expect` / `check` / `starterExpectation` exactly, including whitespace:

| Item | Claimed | Observed | OK |
|------|---------|----------|----|
| S1 ex1 `print("Hello!")` | `Hello!` | `Hello!` | ✓ |
| S1 ex2 two prints | `Code\nLab` | `Code` / `Lab` | ✓ |
| S1 ex3 `print("The answer is", 42)` | `The answer is 42` | same | ✓ |
| S1 ex4 `print(2 + 3)` | `5` | `5` | ✓ |
| S1 ex4 contrast `print("2 + 3")` | `2 + 3` | `2 + 3` | ✓ |
| S1 E1 `print("Code", "Lab")` | `Code Lab` | `Code Lab` | ✓ |
| S1 E2 starter `print(Hello)` | `raises:NameError` | `NameError: name 'Hello' is not defined` | ✓ |
| S1 E2 solution | `Hello` | `Hello` | ✓ |
| S1 E3 solution | `Ready to code\nLet's go` | same | ✓ |
| S2 ex1/2/3 | `Hi` / `Go` / `shown` | same | ✓ |
| S2 E1 | `two\nthree` | `two` / `three` | ✓ |
| S2 E2 starter | runs-clean → `keep` | `keep` | ✓ |
| S2 E2 solution | `Welcome\nkeep` | same | ✓ |
| S3 ex1/2/3 | `a-b` / `done!next` / `red/green/blue` | same | ✓ |
| S3 E1 solution | `a-b` | `a-b` | ✓ |
| S3 E2 starter `sep=)` | `raises:SyntaxError` | `SyntaxError: expected argument value expression` | ✓ |
| S3 E2 solution | `red/green/blue` | `red/green/blue` | ✓ |
| S3 E3 solution | `loading...` | `loading...` | ✓ |

The exact NameError string quoted in S1 E2 hint #1 and the SyntaxError text
implied by S3 E2 hint #1 both match the live 3.13.7 tracebacks. The "read the
real error" habit therefore starts on genuine strings, as the brief requires.

Prose claims are also accurate: default `sep` is one space, default `end` is a
newline, single and double quotes are interchangeable, `#` runs to end of line.
No invented behaviour found.

## 2. Schema invariant compliance

- **Inv. 1** — every example runs clean and matches `expect`. ✓ (verified above)
- **Inv. 2** — every solution passes its own check. ✓
- **Inv. 3** — every starter matches its `starterExpectation` (incl. the two
  raising starters). ✓
- **Inv. 4** — `id` unique; `recalls: []` is correct (Ch.0 is `lang:"none"`; the
  warm-up recalls an *idea*, per brief decision/note 2). No undefined spine
  concept is recalled. ✓
- **Inv. 5** — 3 `codex` entries (≥1) and a `recap` present. ✓
- **Inv. 6** — reading level is the harness's job; prose is flowing Think
  Python / HTTLACS register, complete sentences, no choppy or chatty drift, no
  private scaffolding notation. Subjectively in band. (Author must still confirm
  via `npm test`; see Observation A.)
- **Inv. 7** — `timeBudgetMin: 15` ∈ [15,25]. ✓
- **Inv. 8** — all four misconception ids (`quotes-in-output`,
  `unquoted-word-as-text`, `comment-runs`, `sep-vs-end`) exist in
  `tests/misconceptions.md`; every keyed exercise has specific, non-generic
  hints that match the registered hint angle. ✓
- **Inv. 9** — `NameError` and `SyntaxError` both have translation blocks in
  `src/js/errors.js` (lines 21, 30). ✓
- **Inv. 10 (relaxed lesson-level ramp)** — within-subsection rungs
  non-decreasing: S1 `1→4→5`, S2 `1→4`, S3 `3→5→6`. Final subsection S3's first
  rung (3) ≥ S1's first rung (1). S2 dips deliberately and is not padded, exactly
  as brief decision 3 permits. ✓

## 3. Breadth vs the brief

The brief's concept line and coverage check require: print, arguments, quoted
strings, comments, multi-arg spacing, `sep`, `end`, expressions-as-args,
NameError, SyntaxError. Every one is present:

- expressions-as-args → S1 ex4 (`print(2 + 3)` → `5`) with the `"2 + 3"`
  contrast, per settled decision 1. ✓
- NameError → S1 E2. ✓
- SyntaxError → S3 E2. ✓
- `concepts` array matches the brief verbatim (`print`, `output`,
  `string-literal`, `expression-as-arg`, `comment`, `sep`, `end`). ✓
- glossary, codex, recap.mistakes all match the brief's specified content
  (argument, string-kept-lightweight, comment, separator, end). ✓

Nothing the brief assigns to 1.1 is dropped; nothing owned by a later lesson
(number *types*, operator precedence, `str` as a named type, variable/name
lookup) is leaked in. The S1 E2 NameError framing correctly stays at "text needs
quotes" and does not explain name lookup (brief framing constraint honored). The
`42` argument note correctly defers types to 1.2.

## 4. Structure adherence (CURRICULUM-STRUCTURE.md)

- Lesson shape `Warm-up → 3 subsections → Recap + Codex`. ✓
- Each subsection = one concept, with concept prose + examples + exercises. ✓
- Examples ordered easy→harder; later examples add a wrinkle (multi-arg, then
  expression). ✓
- Exercises fade scaffolding: boilerplate → stub → blank within each subsection. ✓
- At least one debug exercise where the concept can break: S1 E2 (NameError),
  S2 E2 (accidental comment-out), S3 E2 (empty `sep=`). ✓
- Rung ladder uses the canonical labels (predict/fix/complete/write). ✓

## 5. Spiral weaving

- Warm-up recalls the Ch.0 "sequence of exact steps" idea and frames this lesson
  as its first Python form — a genuine retrieval beat, not decorative. ✓
- Intra-lesson reuse: S2 examples reuse `print`; S3 reuses multi-argument `print`
  introduced in S1 (the default-space behaviour is the hook for `sep`). ✓
- This is lesson 1.1, so there is no earlier in-chapter lesson to recall; the
  empty `recalls` is correct rather than a gap.

## 6. Hints keyed to misconceptions

Each keyed exercise's hints are a graduated ladder that nudges toward thinking
without handing over the answer at hint 1, then concretizes by the last hint —
matching AUTHORING's "separate on-demand ladder of genuine nudges." Hint content
aligns with each id's registered hint angle (e.g. `quotes-in-output` → "run it
and look, quotes mark start/end"; `sep-vs-end` → "sep between, end after").
Predict exercises (S1 E1, S2 E1) keep `solution === starter`, correct for a
predict rung. ✓

---

## Observations (non-blocking — no change required)

- **A. Reading level not machine-verified here.** Inv. 6 (grade 7–9, code
  stripped) is enforced by `npm test`; this review did not run the grader. The
  draft's header asserts verification of code but the reading-level grade should
  be confirmed by the harness before the lesson is marked done. Subjectively the
  prose sits in band.
- **B. S1 E1 misconception fit.** S1 E1 (`print("Code", "Lab")` predict) is keyed
  to `quotes-in-output`, and its hint #3 covers quotes-not-shown, so the keying
  is defensible. The exercise's primary teaching point is multi-arg spacing,
  which has no dedicated registered misconception; `quotes-in-output` is the
  closest legitimate fit. Acceptable, not a defect.
- **C. Verification log embedded in the data file.** The draft carries a prose
  "Verification log" and "Schema-invariant notes" after the JS block. These live
  outside the `window.CODELAB.lessons.push({...})` object, so they do not enter
  shipped data; they are author documentation. Harmless. If the build ever
  concatenates the whole `.md`, confirm it extracts only the fenced ```js block.
- **D. `print()` blank-line beat** offered in brief note 6 is not used; that is
  explicitly optional, so its absence is fine.
