/* arena.js — Challenge Arena: unlock logic from concept tags, silhouettes
   for locked items, challenge codes (W4-C2), star scoring (solved / no hints
   / under par), boss timers, coach mode (assign, results view, custom
   challenge authoring with output-match checks). */
